/**
 * Jack The Butler — Plugin Starter
 *
 * Three annotated examples — one per plugin category. Copy the one you need.
 *
 * pms-example.ts     → new PMS integration (Mews, Cloudbeds, Opera, custom)
 * ai-example.ts      → new AI provider (OpenAI-compatible, custom LLM)
 * channel-example.ts → new messaging channel (SMS, WhatsApp, custom)
 *
 * Each file is self-contained — copy just the one that matches your category.
 */

export type { StarterConfig, StarterAdapter } from './pms-example.js';
export type { StarterAIConfig, StarterAIProvider } from './ai-example.js';
export type { StarterChannelConfig, StarterChannelAdapter } from './channel-example.js';
